{
  "dataset": "TODO",
  "model": "TODO",
  "training": {
    "epochs": 100,
    "batch_size": 128,
    "learning_rate": 0.001
  },
  "evaluation": {
    "metrics": ["TODO"],
    "protocol": "TODO"
  },
  "reproducibility": {
    "seed": 1,
    "seeds": [1, 2, 3],
    "notes": "Prefer storing all tunables here and keep CLI overrides minimal."
  }
}

